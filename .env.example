# Server-only (Vercel Environment Variables / vercel dev)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: force a specific model (falls back automatically on 503/429)
# GEMINI_MODEL=gemini-3.6-flash
